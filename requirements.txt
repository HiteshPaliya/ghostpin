# GhostPin Enterprise v5.0 — Python Dependencies
# Core
flask>=2.0
click>=8.0
requests>=2.28

# Optional (enhanced features)
# mitmproxy>=10.0    # for Traffic Replay feature
# reportlab>=4.0     # for PDF report generation (HTML fallback built-in)
